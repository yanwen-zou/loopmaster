# Compatibility entry point for pip-style installs. Prefer the locked uv flow:
#   uv sync
#   uv sync --all-extras --all-groups   # full robot/VR stack
#   uv run python -m unittest discover -s tests

-e .[hei-rebot-lift,vr,dev]
